# Datasets are hundreds of MB and are supplied by the organisers, not us.
data/
train/
Test_NoisyLR/
*.zip
**/GT/
**/NoisyLR/
DIV2K*/

# NOTE: weights/ and outputs/ are deliberately NOT ignored. The hackathon
# requires the trained weights and the restored test outputs to be in the
# repository. They are large binaries, so .gitattributes routes them through
# Git LFS -- see the README.

# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/

# os / editor
.DS_Store
__MACOSX/
._*
.vscode/
.idea/
