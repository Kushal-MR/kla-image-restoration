# Exact environment used for training (Kaggle P100, Python 3.11).
# Regenerate with:  pip freeze > requirements.txt
#
# NOTE: replace this file with the real `pip freeze` output from the Kaggle
# session that produced weights/best.pt before submitting. The hackathon
# requires the complete freeze for reproducibility.
numpy==1.26.4
torch==2.4.0
torchvision==0.19.0
Pillow==10.4.0
matplotlib==3.8.4
lpips==0.1.4          # metrics only; NOT imported by evaluate.py
scikit-image==0.24.0  # metrics only; NOT imported by evaluate.py
tqdm==4.66.4
